# รายงานโครงงาน DormLoop

## 1. ภาพรวมโครงงาน

DormLoop เป็นเว็บตลาดซื้อขายสินค้าสำหรับนักศึกษา โดยออกแบบให้ผู้ใช้สามารถค้นหาสินค้า ดูรายละเอียดสินค้า ลงประกาศขาย และจัดการสินค้าของตนเองได้ในระบบเดียว แนวคิดหลักคือการทำให้การซื้อขายของใช้ภายในชุมชนนักศึกษาสะดวกขึ้น ลดขั้นตอนการติดต่อ และเปิดโอกาสให้นำสิ่งของที่ไม่ได้ใช้แล้วกลับมาใช้ประโยชน์ต่อ

ระบบมีชื่อแบรนด์บนหน้าจอว่า **TSU Market by Students** เพื่อสื่อถึงตลาดที่เกิดจากนักศึกษาและใช้งานโดยนักศึกษา ฟังก์ชันหลักประกอบด้วย:

- แสดงสินค้าและค้นหาสินค้าตามชื่อหรือหมวดหมู่
- ดูรายละเอียด ราคา สภาพสินค้า รูปภาพ และสถานที่นัดรับ
- สมัครสมาชิกและเข้าสู่ระบบ
- ลงประกาศสินค้า แก้ไข และลบสินค้าของตนเอง
- แสดงรายการสินค้าของผู้ขาย
- เพิ่มสินค้าในตะกร้าและตรวจสอบรายการในตะกร้า
- สลับภาษาไทยและภาษาอังกฤษในส่วนติดต่อผู้ใช้
- ตรวจสอบรูปภาพ JPG, PNG และ WebP ขนาดไม่เกิน 2 MB ก่อนบันทึก
- เปลี่ยนสถานะประกาศเป็นพร้อมขาย จองแล้ว หรือขายแล้ว
- เสนอหมวดหมู่ใหม่เพื่อรอแอดมินอนุมัติ
- กรองสินค้าตามช่วงราคาและแสดงสินค้าแนะนำในหมวดหมู่เดียวกัน
- ผู้ใช้ทั่วไปสามารถเพิ่มสินค้าใน guest cart ได้โดยไม่ต้องเข้าสู่ระบบ ส่วนผู้ใช้ที่ login จะมีตะกร้าถาวรแยกตามบัญชี
- ผู้ใช้ที่เข้าสู่ระบบสามารถส่งข้อความสอบถามผู้ขายจากหน้ารายละเอียดสินค้าได้
- สมัครสมาชิกด้วยชื่อ-นามสกุล รหัสนิสิต Email และ Password โดยรหัสนิสิตต้องไม่ซ้ำ

## 2. กลุ่มผู้ใช้และปัญหาที่ต้องการแก้

กลุ่มผู้ใช้หลักคือนักศึกษาที่ต้องการซื้อหรือขายของใช้ในมหาวิทยาลัย เช่น เฟอร์นิเจอร์ อุปกรณ์การเรียน เสื้อผ้า หรือของใช้ส่วนตัว ปัญหาที่ระบบพยายามแก้คือการกระจายข้อมูลสินค้าอยู่ในหลายช่องทาง การค้นหาสินค้าทำได้ยาก และการติดตามว่าสินค้าใดเป็นของผู้ขายคนใดไม่เป็นระบบ

ระบบจึงแบ่งประสบการณ์ออกเป็นสองส่วน:

1. ผู้ซื้อสามารถเรียกดู ค้นหา เปิดดูรายละเอียด และเพิ่มสินค้าในตะกร้าได้โดยไม่ต้องเข้าสู่ระบบ
2. ผู้ขายต้องเข้าสู่ระบบก่อนจึงจะสร้าง แก้ไข หรือลบประกาศของตนเองได้

## 3. การออกแบบระบบ

### 3.1 สถาปัตยกรรม

ระบบใช้สถาปัตยกรรมแบบ client-server แบ่งเป็นสามชั้นหลัก:

- **Frontend:** React และ React Router จัดการหน้าจอและการเปลี่ยนเส้นทาง
- **Backend:** Express จัดการ REST API สำหรับ authentication และสินค้า
- **Database:** SQLite จัดเก็บข้อมูลผ่าน Prisma ORM

ในโหมดพัฒนา Vite ทำหน้าที่เป็น frontend server ที่พอร์ต 3000 และ proxy คำขอ `/api` ไปยัง Express ที่พอร์ต 5000 ทำให้ frontend เรียก API ด้วย path เดียวกันได้ทั้งในเครื่องพัฒนาและเมื่อ deploy หลัง reverse proxy

### 3.2 การจัดการสถานะ

ใช้ React Context แยกความรับผิดชอบของข้อมูลออกเป็นส่วนต่าง ๆ:

- `AuthContext` จัดการผู้ใช้ token การเข้าสู่ระบบ การสมัครสมาชิก และการออกจากระบบ
- `ProductContext` โหลด ค้นหา เพิ่ม แก้ไข และลบสินค้า รวมถึงสถานะ loading
- `CartContext` จัดการรายการสินค้าในตะกร้าและจำนวนสินค้า
- `LanguageContext` จัดการภาษาที่ผู้ใช้เลือก

การแยก context ทำให้หน้าแต่ละหน้าสามารถใช้ข้อมูลร่วมกันได้โดยไม่ต้องส่ง props ผ่านหลายระดับของ component

### 3.3 โครงสร้างข้อมูล

ฐานข้อมูลมีโมเดลหลักสองโมเดล:

- `User` เก็บชื่อ อีเมล รหัสผ่าน บทบาท และความสัมพันธ์กับสินค้าที่ผู้ใช้ลงขาย
- `Product` เก็บชื่อสินค้า รายละเอียด ราคา หมวดหมู่ สภาพ รูปภาพ สถานที่นัดรับ และผู้ขาย

ผู้ใช้หนึ่งคนสามารถมีสินค้าหลายรายการ และสินค้าแต่ละรายการมีผู้ขายหนึ่งคน ความสัมพันธ์นี้ช่วยให้ระบบตรวจสอบสิทธิ์การแก้ไขและลบสินค้าได้

นอกจากนั้น `Product` มีสถานะ `AVAILABLE`, `RESERVED` และ `SOLD` เพื่อสื่อสถานะการขายอย่างชัดเจน ส่วน `Category` ใช้เก็บคำขอหมวดหมู่ใหม่ โดยคำขอจะเริ่มต้นเป็น `approved = false` และจะปรากฏในตัวเลือกสินค้าเมื่อแอดมินอนุมัติแล้ว `CartItem` เชื่อม `User` กับ `Product` และมี unique key คู่ `userId` กับ `productId` จึงไม่สามารถอ่านหรือแก้ไขตะกร้าของบัญชีอื่นผ่าน API ได้ ขณะที่ guest cart แยกเก็บในเครื่องและไม่สามารถเข้าถึงข้อมูล cart ของผู้ใช้ที่ login ได้

### 3.4 การยืนยันตัวตนและสิทธิ์

การสมัครสมาชิกจะ hash รหัสผ่านด้วย bcrypt ก่อนบันทึกลงฐานข้อมูล ส่วนการเข้าสู่ระบบจะสร้าง JSON Web Token ที่มีข้อมูลผู้ใช้และบทบาท token ถูกส่งกลับไปยัง frontend และจัดเก็บไว้ใน local storage เพื่อใช้กับคำขอที่ต้องมีสิทธิ์

API สำหรับสร้าง แก้ไข และลบสินค้าตรวจสอบ Bearer token ก่อนดำเนินการ และอนุญาตให้เจ้าของสินค้า หรือผู้ใช้ที่มีบทบาท admin จัดการรายการนั้นได้

## 4. การออกแบบส่วนติดต่อผู้ใช้

การออกแบบเน้นความเรียบง่ายและเหมาะกับการใช้งานซ้ำบ่อย โดยมี navbar เป็นจุดนำทางหลักไปยังหน้าร้านค้า ตะกร้า การเข้าสู่ระบบ และการลงขายสินค้า หน้าร้านค้าแสดงข้อมูลที่จำเป็นต่อการตัดสินใจ เช่น ชื่อ ราคา หมวดหมู่ สภาพ และผู้ขาย

ระบบใช้สีและ spacing ที่สม่ำเสมอ รวมถึง icon จาก `lucide-react` เพื่อช่วยลดข้อความที่ไม่จำเป็น การออกแบบรองรับทั้งภาษาไทยและภาษาอังกฤษผ่านปุ่มเปลี่ยนภาษาใน navbar

เส้นทางสำคัญของระบบ ได้แก่:

- `/` หน้าแรก
- `/shop` หน้าร้านค้า
- `/product/:id` รายละเอียดสินค้า
- `/cart` ตะกร้า
- `/login` และ `/register` การยืนยันตัวตน
- `/my-products` รายการสินค้าของผู้ใช้
- `/product/create` และ `/product/edit/:id` จัดการประกาศสินค้า

## 5. การทดสอบและผลลัพธ์

การตรวจสอบระบบครอบคลุมทั้งการเริ่มต้น server และการเชื่อมต่อระหว่าง frontend กับ backend:

- backend เริ่มทำงานที่พอร์ต 5000 ได้สำเร็จ
- endpoint `/` ตอบกลับสถานะ `200` พร้อมข้อความยืนยันว่า API ทำงานอยู่
- endpoint `/api/products` อ่านข้อมูลจาก SQLite และตอบกลับสถานะ `200`
- Vite proxy ส่งคำขอจาก `http://localhost:3000/api/products` ไปยัง backend ได้สำเร็จ
- คำสั่ง `npm run dev:all` เริ่ม frontend และ backend พร้อมกันได้
- คำสั่ง `npm run build` สร้าง production bundle ของ frontend สำเร็จ
- การค้นหาจากชื่อหรือคำอธิบายและการกรองช่วงราคาตอบกลับสำเร็จ
- สินค้าเกิน 2 MB ถูกปฏิเสธด้วยข้อความแจ้งเตือนที่เข้าใจง่าย
- ผู้ใช้ที่ไม่ใช่เจ้าของสินค้าได้รับ `403 Forbidden` เมื่อพยายามแก้ไขสินค้า
- การเปลี่ยนสถานะสินค้าและการโหลดสินค้าแนะนำทำงานผ่าน API สำเร็จ
- ผู้ใช้ A และผู้ใช้ B มีรายการในตะกร้าแยกกัน และผู้ใช้ที่ไม่ login ได้รับ `401 Unauthorized`
- ผู้ซื้อส่งข้อความถึงผู้ขายได้ และระบบปฏิเสธผู้ไม่ login หรือการส่งข้อความหาตัวเอง
- การสมัครสมาชิกใหม่ผ่านเมื่อข้อมูลครบ และระบบตอบ `409 Conflict` เมื่อรหัสนิสิตซ้ำ

ปัญหาที่พบระหว่างการทดสอบคือ backend ไม่สามารถเริ่มได้เมื่อยังไม่ได้ติดตั้ง dependencies ในโฟลเดอร์ `server` ทำให้ Vite แสดง `ECONNREFUSED` วิธีแก้คือเพิ่มขั้นตอนติดตั้ง server dependencies และ generate Prisma Client ไว้ใน README อย่างชัดเจน

## 6. การสะท้อนผลการเรียนรู้

การพัฒนาโครงงานนี้ทำให้เข้าใจการทำงานร่วมกันระหว่าง frontend และ backend มากขึ้น โดยเฉพาะการแยก port, การใช้ Vite proxy และการติดตาม error จากชั้น frontend ย้อนกลับไปถึง process ของ backend การเห็น `ECONNREFUSED` ทำให้เรียนรู้ว่าควรตรวจสอบว่า service ปลายทางกำลังทำงานอยู่หรือไม่ก่อนแก้โค้ดส่วน UI

ด้าน React ได้ฝึกการออกแบบ component และการจัดการ state ที่ใช้ร่วมกันผ่าน Context API รวมถึงการป้องกัน route ที่ต้องเข้าสู่ระบบ ด้าน backend ได้เรียนรู้การสร้าง REST API การตรวจสอบ token การ hash รหัสผ่าน และการใช้ Prisma เชื่อมต่อ relational data กับ SQLite

สิ่งที่ควรพัฒนาต่อคือการย้าย secret และค่าการเชื่อมต่อทั้งหมดไปยัง environment variables การเพิ่ม validation ที่ละเอียดขึ้น การเขียน automated tests สำหรับ API และการเพิ่มระบบแชตหรือการนัดหมายระหว่างผู้ซื้อกับผู้ขาย นอกจากนี้ควรเพิ่มการจัดการ loading และ error state ให้สื่อสารกับผู้ใช้ได้ชัดเจนยิ่งขึ้น

## 7. สรุป

DormLoop แสดงแนวทางการสร้างเว็บ marketplace ขนาดเล็กที่เชื่อมต่อ frontend, backend และฐานข้อมูลเข้าด้วยกันอย่างเป็นระบบ จุดสำคัญของโครงงานไม่ได้อยู่ที่การแสดงสินค้าเพียงอย่างเดียว แต่รวมถึงการกำหนดสิทธิ์ผู้ใช้ การจัดการข้อมูลสินค้า และการออกแบบ workflow ให้ผู้ซื้อและผู้ขายใช้งานได้ต่อเนื่องในระบบเดียว
